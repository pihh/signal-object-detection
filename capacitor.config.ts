import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vision.unconvetional',
  appName: 'unconventional-vision',
  webDir: 'www'
};

export default config;
